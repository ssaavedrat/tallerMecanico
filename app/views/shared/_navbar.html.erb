<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <a class="navbar-brand" href="#">🔧 El Tuercas 🔧</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <!-- Menú de navegación -->
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Inicio", root_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "El Taller Mecánico", about_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Contacto", new_contact_path, class: "nav-link" %>
        </li>
      </ul>
    </div>
    <div>
      <ul class="navbar-nav">
        <%if user_signed_in?%>
          <div class="dropdown">
            <button class="btn dropdown-toggle" type="button" id="triggerId" data-bs-toggle="dropdown" aria-haspopup="true"
                        aria-expanded="false">
              <%if current_user.name%>
                Hola <%= current_user.name %>
              <%else%>
                <%= current_user.email %>
              <%end%>
            </button>
            <div class="dropdown-menu" aria-labelledby="triggerId">
              <%# destroy %>
              <%= button_to "Cerrar sesión", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </div>
          
        <% else %>
          <li>
            <%= link_to 'Iniciar sesión', new_user_session_path, class: 'text-decoration-none text-black' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
